1. I have a bunch of stuff and I need to group them together, that's basically what a list is. I'd use a list if I've got a bunch of things I need to keep track of like, say, people's names and their grades, and I might want to change some of that info later.

 2. So, a tuple is like a list, but once you make it, you can't change it. A list uses square brackets and a tuple uses parentheses.

 3. A set just holds a bunch of unique items and doesn't care which order they're in. Lists let you have the same thing multiple times and remember where everything is.

 4. If something is "mutable", that means you can modify it after it's already been made by adding or taking away pieces, or changing what they are.

 5. If something is "immutable", that means you can't change it at all once it's been made, instead, you'd just make a brand new one.

6. The len() function will tell you how many items are in a bunch, like a list or a string or whatever.

 7. I'd use the.get() method with a dictionary because if the key doesn't actually exist, it won't crash.

8. No, there isn't a.length() in Python, you should use len().

9. The.keys() method will get you all the keys in a dictionary.

 10..values() will get you all the actual values in a dictionary.

 11..remove() will take away the very first one in the list that matches what you tell it to look for.

 12..pop() is kind of like.remove(), but it gives back the thing it took out and it finds it by its position, not its value.

 13. When you do.sort() on a list, it organizes the stuff inside it in order, which is probably either in size or alphabetically, depending on what it is.

 14..append() adds a new thing to the end of the list.

 15..insert() is like.append(), but you can pick exactly where in the list it goes.

 16. A class is essentially a plan for what an object should have and what it can do.

 17. An object is what you make from a class. The class is the instructions and the object is the actual thing.

 18. Abstraction is essentially about hiding the parts of the program that aren't important and just showing the important parts, this makes it more user-friendly and easier to use.

 19. Encapsulation is when you bundle up an object's info and the code that deals with that info and it stops other parts of the program from messing with it the wrong way.

 20. Inheritance is when one class can use all the features of another class without having to write the same code again.

 21. If two or more classes both have a method with the same name, but each one does something different, that's called polymorphism.

 22. A constructor is a special method that sets up a new object when you make one, and it gets used when you create a new object.

 23. A tuple can't be changed once it's been made.

 24. A set will automatically get rid of any duplicate entries.

 25. Lists are for when you have a bunch of stuff and you know where each piece is (by its number), while a dictionary stores things by matching a word (the key) with its definition (the value). 

26. If you want to add a new thing to the end of a list, you would use.append()

 27. To look up a value by its key in a safe way (without getting an error if the key doesn't exist), you would use.get()

 28. A class is like the plan, and the actual thing made from that plan is the object.

 29. So, basically, with OOP, you can share code through inheritance, and then use the same method in lots of different ways with polymorphism.

 30. A good constructor makes sure your new object is set up right and ready to go from the start.

Add class comment…

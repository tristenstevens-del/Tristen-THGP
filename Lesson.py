Data Structure:built-in data structures (provided natively by Python) and user-defined data structures (built manually using classes or imported modules).
List:a built-in, mutable, and ordered collection of items wrapped in square brackets
Tuple:a built-in data collection type that is ordered, unchangeable (immutable), and allows duplicate values. 
Set:a built-in data structure used to store an unordered collection of unique items.
Dictionary:a built-in data structure that stores data in ordered, mutable, and unique key-value pairs
Mutable:an object whose internal state or contents can be modified after it is created
Immutable:immutable object is an object whose internal state or value cannot be modified after it is created.
Len():a built-in utility used to return the number of items (length) inside an object.
.get():most commonly refers to the dict.get() method used to safely retrieve values from a dictionary, or the requests.get() function used to send HTTP GET requests to websites.
.length():In Python, you find the length of an object using the built-in len() function.
.keys():In Python, dictionary keys act as the unique indices used to retrieve, store, and manage values inside a dictionary (dict) object
.values():any piece of data that a program manipulates, such as a number, text string, or collection
.remove():delete by value, by index position, or clear everything.
.pop():pop() is a built-in method used to remove and return an element from a collection
.sort():use the list.sort() method to modify a list in place, or the sorted() function to return a new sorted list from any iterable
.append():adds a single item to the very end of an existing list
.insert():list.insert(index, element) method. [1]
Object Oriented Programming (OOP):a programming paradigm that organizes code into reusable blueprints called Classes, which are used to instantiate individual Objects containing data (attributes) and actions (methods). 
Class:a class is a blueprint or template used to create objects, bundling data (attributes) and behavior (methods) into a single reusable unit
Object:an object is an instance of a class that encapsulates data (attributes) and behavior (methods) into a single entity
Abstraction:the process of hiding complex internal implementation details and exposing only the essential, high-level features to the user
Encapsulation:the Object-Oriented Programming (OOP) practice of bundling data (attributes) and methods (functions) into a single unit (a class) while restricting direct access to some of the object's components
Inheritance:allows a child class (subclass) to adopt all the attributes and methods of a parent class (base class)
Polymorphism:the ability of different objects, functions, or operators to respond to the same interface or method call in their own unique way.
Constructor:a special method automatically invoked when you create a new instance of a class, primarily used to initialize the object's attributes
Dunder methods:are special built-in methods in Python that start and end with two underscores, such as __init__ or __str__
